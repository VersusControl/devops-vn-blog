# Example values — in a real project pass these via environment variables
# (TF_VAR_postgres_username / TF_VAR_postgres_password) or a secrets manager,
# never commit real secrets.
postgres_username = "secret-username"
postgres_password = "secret-password"
